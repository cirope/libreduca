<%= content_for :form_actions do %>
  <%= link_to t('label.show'), [@forum.owner, @forum] %> |
  <%= link_to t('label.list'), polymorphic_path([@institution, Forum]) %>
<% end %>

<%= render 'form' %>
