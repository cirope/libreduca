<ul class="breadcrumb">
  <li>
    <%= link_to_if can?(:read, @school), @school.to_s, @school %>
    <% unless @grade.new_record? %>
      <span class="divider">/</span>
    <% end %>
  </li>
  <% unless @grade.new_record? %>
    <li class="active"><%= @grade %></li>
  <% end %>
</ul>