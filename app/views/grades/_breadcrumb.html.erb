<ul class="breadcrumb">
  <li>
    <%= link_to @school.to_s, @school %>
    <% unless @grade.new_record? %>
      <span class="divider">/</span>
    <% end %>
  </li>
  <% unless @grade.new_record? %>
    <li class="active"><%= @grade %></li>
  <% end %>
</ul>