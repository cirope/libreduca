<div class="readonly-data">
  <h2><%= @survey %></h2>

  
</div>
<div class="form-actions">
  <%= link_to t('label.edit'), polymorphic_path([@teach || @content, @survey], action: :edit) if can? :update, @survey %> |
  <%= link_to t('label.list'), polymorphic_path([@teach || @content, Survey]) %>
</div>
