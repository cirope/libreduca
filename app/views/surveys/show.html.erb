<div class="readonly-data">
  <h2><%= @survey %></h2>

  
</div>
<div class="form-actions">
  <%= link_to t('label.edit'), edit_teach_survey_path(@teach, @survey) if can? :update, @survey %> |
  <%= link_to t('label.list'), teach_surveys_path(@teach) %>
</div>
