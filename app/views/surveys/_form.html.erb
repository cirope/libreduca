<%= simple_form_for([@content, @survey]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, input_html: { class: 'span11', autofocus: true } %>

    <%= f.simple_fields_for :questions do |q_f| %>
      <%= render 'question', f: q_f %>
    <% end %>

    <%= link_to_add_fields t('view.contents.surveys.new_question'), f, :questions %>
  </div>

  <div class="form-actions">
    <div class="pull-right"><%= yield :form_actions %></div>
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
