<%= render 'breadcrumb', :@teach => @content.teach if @content %>

<%= content_for :form_actions do %>
  <%= link_to t('label.back'), edit_teach_content_path(@content.teach, @content) %>
<% end %>

<%= render 'form' %>
