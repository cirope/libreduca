$('#new-presentation-for-homework-<%= @homework.to_param %>').html(
  '<%= j render(template: @presentation.persisted? ? 'presentations/show' : 'presentations/new') %>'
).effect('highlight', 'slow');
