<% if @comment.persisted? %>
  $('#comments-header').after('<%= j render(@comment) %>');

  $("#<%= @comment.anchor %>").effect('highlight', 'slow');

  window.location.hash = '<%= @comment.anchor %>';
<% end %>

$("#new_comment").replaceWith('<%= j render('comments/form') %>');
