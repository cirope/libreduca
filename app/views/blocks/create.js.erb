$('#new_block').replaceWith('<%= j render(template: @block.persisted? ? 'blocks/show' : 'blocks/new', formats: [:html]) %>');
$('#block_content').focus();
$('#block-<%= @block.to_param %>').effect('highlight', 'slow');
