Hola <%= @resource.name %>,
================================================================================

Alguien ha pedido cambiar tu contraseña, su código de seguridad es:

<%= @resource.reset_password_token %>

--------------------------------------------------------------------------------
Si no lo pediste, puedes ignorar este correo.

Tu contraseña no cambia a menos que uses el código anterior.
