<% @reply = @question.reply_by(current_user) || @reply %>

<%= render 'replies/panel' %>

<% unless reply_rendered_inline? %>
  <div class="form-actions">
    <%= link_to_edit edit_question_reply_path(@question, @reply) if can? :update, @reply %> |
    <%= link_to t('label.list'), question_replies_path(@question) %>
  </div>
<% end %>
