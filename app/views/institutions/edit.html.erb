<%= content_for :form_actions do %>
  <% if can? :read, @institution %>
    <%= link_to t('label.show'), @institution %> |
  <% end %>
  <% if can? :read, Institution %>
    <%= link_to t('label.list'), institutions_path %>
  <% end %>
<% end %>

<%= render 'form' %>
