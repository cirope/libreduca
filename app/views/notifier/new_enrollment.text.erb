<%= t('notifier.new_enrollment.greeting.text', user: @enrollment.enrollable.name) %>
================================================================================

<%= t(
  'notifier.new_enrollment.teach.text',
  course: @enrollment.course
) %>
--------------------------------------------------------------------------------

<%= t(
  'notifier.new_enrollment.teach_details.text',
  start: l(@enrollment.teach.start, format: :long),
  finish: l(@enrollment.teach.finish, format: :long)
) %>

<%= render 'footer' %>
