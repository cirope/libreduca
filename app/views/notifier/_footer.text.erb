--------------------------------------------------------------------------------
<%= t(
  'notifier.footer.instructions.text',
  institution: @institution,
  link: root_url(subdomain: @institution.identification)
) %>
