<%= t('notifier.new_comment.greeting.text', commentable: @commentable) %>
================================================================================

<%= t('notifier.new_comment.user_say.text', user: @comment.user) %>
--------------------------------------------------------------------------------

<% @comment.comment.each_line do |l| %>
  | <%= l.chomp %>
<% end %>

<%= t('notifier.new_comment.view') %>: <%= polymorphic_url([@commentable.owner, @commentable], anchor: @comment.anchor) %>

<%= render 'footer' %>
