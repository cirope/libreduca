<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <div class="well">
      <h3>¿Olvidaste tu contraseña?</h3>

      <p class="text-muted">
        Ingresa debajo tu dirección de correo y te enviaremos instrucciones para
        restablecerla
      </p>

      <%= simple_form_for(resource, as: resource_name,
          url: password_path(resource_name), html: { method: :post }) do |f| %>

        <div class="form-inputs">
          <%= f.input :email, label: false, input_html: { autofocus: true } %>
        </div>

        <div class="form-actions">
          <%= submit_tag(
            t('password_resets.new.send_instructions'), class: 'btn btn-default'
          ) %>
        </div>
      <% end %>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <p><%= link_to t('label.back'), root_path %></p>
  </div>
</div>
