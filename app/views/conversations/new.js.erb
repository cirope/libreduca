$('#<%= @conversable.anchor %>').popover({
  html: true,
  title: 'Para <%= @conversable.user.name %>',
  content: '<%= j render("form") %>',
  placement: 'top'
}).popover('show');

$('#<%= @conversable.anchor %>').removeAttr('data-remote');
$('#<%= @conversable.anchor %>').click(function(){ return false; });
$('#<%= @conversable.anchor %>').siblings('.popover').find('textarea').focus();
